import { HeroDesignDirective } from './hero-design.directive';

describe('HeroDesignDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroDesignDirective();
    expect(directive).toBeTruthy();
  });
});
